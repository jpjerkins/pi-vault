# /etc/tmpfiles.d/vault-t2.conf
#
# Creates the FUSE mount point on boot. /run is a tmpfs that is cleared on
# reboot, so this directory would otherwise be missing after every restart.
#
# See tmpfiles.d(5) for format details.

d /run/vault-t2-fs 0755 philj philj -
